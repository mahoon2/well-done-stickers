rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to stickers collection
    match /stickers/{document} {
      allow read, write: if true;
    }
  }
}